#include<stdio.h>
int main()
{
    float side;
    printf("Enter the length of side: ");
    scanf("%f",&side);
    float area = (side* side);
    printf("\n The area of the square is: %f",area);
}
